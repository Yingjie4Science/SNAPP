# Lightweight, exact CI environment for smoke and committed-artifact QA.
# The full geospatial/model environment is locked under environment-locks/.
numpy==2.4.6
openpyxl==3.1.5
pandas==3.0.3
pytest==9.1.1
pyyaml==6.0.3

# --- NDVI download + clip (src/inputs/ndvi/alternatives) ---
requests>=2.31
xarray>=2023.1
netCDF4>=1.6
python-dotenv>=1.0

# --- NDVI compositing (src/inputs/ndvi/composite_ndvi.py) — needs GDAL ---
rioxarray>=0.15
rasterio>=1.3

# --- Model input builders (src/inputs) ---
geopandas>=0.14          # AOI + prevalence vectors
pandas>=2.0              # CDC PLACES join
openpyxl>=3.1            # read MEPS .xlsx (extract_meps_cost.py)
xlrd>=2.0                # read Census delineation .xls (build_metro_counties --crosswalk)

# --- Sentinel-2 10m NDVI via CDSE openEO (src/inputs/ndvi/ndvi_sentinel2.py) ---
openeo>=0.30

# --- Sentinel-2 10m NDVI via Google Earth Engine (src/inputs/ndvi/ndvi_gee.py) ---
earthengine-api>=0.1.390
geemap>=0.30

# --- Config, tests, plotting ---
pyyaml>=6.0              # read config.yaml
pytest>=7.0              # smoke tests (tests/)
matplotlib>=3.7          # optional maps in summarize_results.py

# --- Urban Mental Health model (src/urban_mental_health) ---
# Heavy: depends on GDAL binaries. conda-forge is the recommended route
# (and the only easy one on Apple Silicon), in its own env:
#   conda create -n invest -c conda-forge python=3.11 natcap.invest
# pip works only if a GDAL toolchain is already present:
# natcap.invest>=3.19
